# IDE / editor scratch files
node_modules/
.DS_Store
Thumbs.db

# Local workspace data — not source
.workbuddy/

# Release artifact — uploaded to GitHub Releases manually
ext-release.zip

# Loadable builds, produced from a commit with git archive (never committed)
dist-firefox.xpi
dist-chromium.zip
